/**
 * joining array / converting array into string using + & , operator 
 * you can convert array to string using toString() function
*/

function joinArr(arr,oper)
{
    return arr.join(oper)
}

console.log(joinArr(["red","green","white","black"],","))